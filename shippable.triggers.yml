triggers:
   - name: sh-trigger
     type: trigger
     version:
       counter: 281111

   - name: releaseTrigger
     type: trigger
     version:
       message: 'create a release from staging cluster. triggered:1'
