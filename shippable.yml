language: node_js

build:
  ci:
    - echo "Hello worl"
    - MYVERSION=`date +%s`

integrations:
  notifications:
    - integrationName: slackInt
      type: slack
      recipients:
        - "#slackchannel"
      on_success: always
  key:
    - integrationName: pemkey
      type: pem-key