language: node_js

build:
  ci:
    - echo "Hello worl"
    - MYVERSION=`date +%s`

integrations:
  notifications:
    - integrationName: slackInt
      type: slack
      recepient:
        - "#slackchannel"
      on_success: always