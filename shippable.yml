language: none

build:
  pre_ci:
    - echo 'CI Running'
    #- export IMAGE_NAME=shippablesamples/samplepipelinesdemo
    - jfrog rt mv my-npm/project/ test-local/project/

integrations:
  hub:
    - integrationName: jfInt
      type: artifactory
#  notifications:
#    - integrationName: "triggerPipelinesDemo"
#      type: webhook
#      payload:
#        - versionName=$BRANCH.$BUILD_NUMBER
#      on_success: always
#      on_failure: never